dist: xenial
language: python
python:
    - "3.6"
    - "3.7"

install:
    - pip install pipenv
    - pipenv install --dev

script:
    - pipenv run pytest

after_success:
    - sphinx-apidoc -f -o docs/source/apis predictiveness_curve
    - cd docs
    - make html

